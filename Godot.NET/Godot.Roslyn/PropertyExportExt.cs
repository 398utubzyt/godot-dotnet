namespace Godot
{
    public enum PropertyExportExt : uint
    {
        None = 0,
        Export = unchecked((uint)(-1)),
        Category = unchecked((uint)(-2)),
        Group = unchecked((uint)(-3)),
        Subgroup = unchecked((uint)(-4)),
    }
}
